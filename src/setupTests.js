import '@testing-library/jest-dom';
// import { browser } from './mocks/browser';

// beforeAll(() => browser.listen())

// afterEach(() => browser.resetHandlers())

// afterAll(() => browser.close())